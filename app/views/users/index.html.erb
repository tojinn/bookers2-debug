<%= render 'layouts/errors', obj: @books %>
<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
     <h2>User info</h2>
     <table class='table'>
  <tr><%= attachment_image_tag(@books.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg") %></tr>
  <tr>
	  <th>name</th>
	  <th><%= @user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= @user.introduction %></th>
  </tr>
</table>

<div class='row'>
  <%= link_to '',edit_user_path(@user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{@books.user}" %>
</div>
     <h2 class="mt-3">New book</h2>
     <%= render 'books/form', book: @books %>
    </div>
    
    <div class="col-md-8 offset-md-1">
     <h2>Users</h2>
     <%= render 'index',users: @users %>
   </div>
 </div>
</div>